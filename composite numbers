int main() {
 int num[]={1,2,3,4,5,6,7,8,9};
 int i;
 int count = 0;

 for(i=1; i<=num; i++)
 {
  if(num % i == 0)
   count++;
 }

 if(count > 2)
  printf("%d is a composite number", num);
 else
  printf("%d is not a composite number", num);
 return 0;
}
